class ListNode{}

class LikedList<T> {
    private root?: ListNode;
    private length: 0;

    add(value: T){
        
    }
}

const numberList = new LikedList<number>();
const stringList = new LikedList<string>();